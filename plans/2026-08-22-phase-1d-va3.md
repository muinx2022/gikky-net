# Plan con — Phase 1d, LƯỢT VÁ 3 (lượt CUỐI)

> Nguồn: phản biện vòng 3 (0 nghiêm trọng · 4 VỪA · 7 NHỎ) + 1 phát hiện thêm của nghiệm thu
> vòng 3. Ngày 2026-08-22. Cả khối 1d + vá1 + vá2 **chưa commit**.

## 0. Đây là lượt CUỐI của Phase 1d

Ba vòng chấm: vòng 1 (8 lỗi) → vòng 2 (1 NẶNG + 4 VỪA + 9 NHỎ) → vòng 3 (**0 nghiêm trọng**,
4 VỪA + 7 NHỎ, ba trong bốn cái VỪA là lỗi *của chính lượt vá trước*). Đang hội tụ.
Xong lượt này thì đóng phase và commit — nên **đừng mở mặt trận mới**. Thấy việc đáng làm mà
ngoài danh sách thì **ghi nợ có tên**, đừng làm.

## 1. Mười một hạng mục

### X1 — W4 để nguyên cửa anh em cách đó 8 dòng, CÙNG FILE *(VỪA)*
`apps/web/app/u/[username]/page.tsx:98` — `"Chưa đăng mạch nào."` là **đúng cái khẳng định**
mà W4 vừa xoá ở dòng 90, vì `users.py:73` lọc `hidden_at__isnull=True` nên user bị mod ẩn sạch
bài sẽ có `machs = []`.
Ca dễ gặp hơn ca dựng riêng: user chỉ bình luận + có 3 mạch bị ẩn ⇒ `da_hoat_dong = true`, khối
chỉ số hiện, và câu sai vẫn in.
⇒ Câu chỉ nói về **màn hình**, đúng nguyên tắc của W4.
Và sửa cả chỗ làm nó lọt: `apps/web/e2e/vo-reddit.spec.ts:632-635` cấm cụm `"chưa đăng"` **chỉ
trong thẻ `cho_trong`**, còn chuỗi ấy nằm ngoài thẻ đó. **Mở phạm vi bốn cụm cấm ra cả `main`.**

### X2 — W7 sửa một lời khẳng định sai bằng một lời khẳng định sai MỚI *(VỪA)*
`api/core/doc_noi_dung.py:264-266` nói phép quét 40 401 cặp *"nằm ở `api/tests/test_pha_hoa_wilson.py`"*.
**File đó không có phép quét nào** — không `range`, không vòng lặp trên `[0,200]`; `grep "40401"`
toàn repo = 0 hit.
Các **con số thì đúng** (cả phản biện lẫn nghiệm thu đã tính lại độc lập, khớp từng chữ số).
Hậu quả thật: **không bài đo nào ghim `CHU_SO_SO_WILSON = 12`** — đổi 12 → 8 làm `(134,15)` và
`(178,21)` gộp làm một mà không gì đỏ.
⇒ **Đưa phép quét thật vào** `test_pha_hoa_wilson.py` (hàm thuần, ~40k phép tính, dưới 1 giây) và
ghim `sàn > 10^-CHU_SO_SO_WILSON`. Đừng chỉ sửa câu chữ — chỗ docstring trỏ tới phải có thật.

### X3 — W3 lớp 1: luật D1 không có bài đo, và hai lớp đang che nhau *(VỪA)*
Phản biện đo: gỡ luật D1 (`mau-token.spec.ts:153-161`) ⇒ **139 vẫn xanh**. Nghiệm thu tách hai
lớp, đo độc lập, cùng kết luận: **lớp 1 mù với D2 và D3**; ma trận 11 biến thể xanh gần như hoàn
toàn nhờ lớp 2 (băm), vì mọi biến thể đều sửa chữ bên trong 9.1.
⇒ Ba việc:
1. **Bài đo gọi thẳng `docMucDongDau`**: D1 ⇒ `toThrow`; V1–V5, V7 ⇒ nhận ra mục lạ. Tách khỏi
   `hangRaoDo`, đúng cách file này đã làm cho ba ca fail-closed ở `:491`.
2. **Nói ra sự thật về phân vai**: lớp 1 **không** bắt được D2/D3 — đó là việc của lớp 2, và **gỡ
   lớp 2 là mở lại hai cửa đó**. Viết vào docstring, vì hôm nay không có gì nói cho người sau biết.
3. `mau-token.spec.ts:109-110` khẳng định *"mọi lối viết chưa ai nghĩ ra đều bị chặn, kể cả lối
   thứ mười hai"* — chỉ đúng **trong** mục 9.1. Vai đóng dấu viết ở `9.2` hay `5.6` thì cả hai lớp
   đều im. Sửa câu cho khớp phạm vi thật.

### X4 — bia mộ lọt "Câu đáng đọc", và câu biện minh sai *(VỪA — lỗi sản phẩm thật)*
`api/core/doc_noi_dung.py:541-545` viết: bia mộ *"chỉ lọt vào tập khi mạch có dưới 10 thread"*.
Sai. Nhóm đáy `up = 0` phá hoà bằng `score`, mà bia mộ có `score = 0` ⇒ nó **thắng** mọi câu bị
dìm và **hoà** với mọi câu chưa ai vote (rồi tuổi quyết).
**Ca:** mạch 20 thread gốc, 5 thread có vote, 1 bia mộ cũ, 14 thread chưa ai vote ⇒ bia mộ lọt
top-10 ⇒ `tong_thread = 10 < 20` ⇒ khối render ⇒ `[bình luận đã xoá]` in ngay dưới nhãn
"Câu đáng đọc" và dòng "…những câu được đánh giá cao nhất". Điều kiện chỉ là **≥ 11 thread gốc mà
dưới 10 thread có vote** — hình dạng bình thường của mạch đông bình luận, ít vote.
⇒ Loại bia mộ khỏi **vế top-10** bằng điều kiện **tường minh**.
⚠ **Chỉ vế top-10.** Bia mộ **đã được trích** vẫn phải ở lại qua vế `đã trích` — đó là luật
`giu_vi_da_trich` của PLAN 5.6, và 1b từng làm hỏng đúng chỗ này.

### X5 — `MA_LOI_THEO_THAM_SO` không có chuông *(nghiệm thu vòng 3 nêu)*
`api/api/loi.py:56` giữ đúng mã lỗi hôm nay (đã gọi thật, xác nhận). Nhưng không test nào khẳng
định "mọi tham số `Literal` có mã lỗi riêng đều phải có mặt trong bảng". Thêm một `Literal` thứ
hai ⇒ mã riêng rơi về `tham_so_khong_hop_le` **im lặng** — đúng loài lỗi W9 dựng bảng này để chặn.
⇒ Hàng rào đọc `openapi.json`: mọi query param có `enum` phải **hoặc** có mặt trong bảng, **hoặc**
nằm trong một danh sách khai tường minh "cố ý dùng mã mặc định".
Ghi luôn: khi hai tham số cùng sai (`?sort=top&limit=abc`) mã trả về phụ thuộc thứ tự — nói ra.

### X6 — `data-trich-binh-luan-id` mang tên sai việc
Nó nay gánh cả **lát cắt ngăn kéo** (`the-moc.tsx:150`, `datNeo` mặc định `false`) — thứ không
phải trích. `mach-can.spec.ts:398` đang đọc thuộc tính tên "trích" để lấy nội dung ngăn kéo, tức
đọc ngược nghĩa. Đổi sang tên trung tính.

### X7 — C5 khẳng định dương tự nghiệm
`vo-reddit.spec.ts:331` dùng chính `dongSoMachSub()` làm oracle. Không rỗng hẳn (có
`toContainText("lập")` + `not.toContainText("mới")` bên cạnh) nhưng vế dương thì tự so với mình.
Thay bằng chuỗi văn học cứng.

### X8 — docstring `_rank_goc` còn một câu chỉ đúng một nửa
`doc_noi_dung.py:308-310`: bia mộ *"đứng trên câu −20 và dưới câu +3, y hệt mọi nút khác"* — chỉ
đúng **ngoài** cửa sổ 48h; một câu `−20` vừa viết ăn `HE_SO_TUOI = 0.15` và vượt bia mộ. Câu ngay
dưới lại thừa nhận hệ số tươi không bị cắt ⇒ hai câu tự mâu thuẫn.

### X9 — prop chết trong `feed.tsx`
Trang sub truyền cả `header` lẫn `tieuDe`/`lede`; có `header` thì `lede={chi_tiet.mo_ta}` không
render ở đâu — bản sao thứ hai của `mo_ta` không ai đọc.

### X10 — docstring migration `0005` lẫn git với DB
Câu "chưa commit nên không cần `0006`" nói về **git**. DB nào đã chạy `0005` bản cũ vẫn giữ index
`mach_created_diem` mà Django không biết (Django so model với file migration, không so với DB).
`gikky_dev` đã kiểm: không còn index đó, không ảnh hưởng thật. Chỉ cần nói cho đúng.

### X11 — dọn nợ có tên
Gom mọi nợ Phase 1d vào **một** chỗ đọc được (cuối `plans/2026-08-22-phase-1d-vo-reddit.md` hoặc
một mục trong file này — thợ chọn, miễn **một** chỗ), để chặng 5 chép thẳng vào thông điệp commit:
keyset khoá biến đổi · tranh chấp khoá `_dong_bo_diem_bai_goc` · nav `chrome.tsx` · chưa có index
phục vụ cả lọc lẫn sắp cho "top theo khoảng" · `BAM_MUC_91` là chữ ký người · lớp 1 mù D2/D3 ·
`wilson_lower_bound` docstring · `?dang_doc` thêm một round-trip · mật độ dòng không có tiêu chí đo.

## 2. Tiêu chí nghiệm thu

| # | Tiêu chí | Cách đo |
|---|---|---|
| D1 | X1: user bị ẩn sạch bài **không** bị nói là chưa từng đăng, ở **cả hai** chỗ; bốn cụm cấm quét cả `main` | test dựng ca; mutant trả câu cũ ⇒ ĐỎ |
| D2 | X2: phép quét thật có trong `test_pha_hoa_wilson.py`; **`CHU_SO_SO_WILSON` 12 → 8 ⇒ ĐỎ** | thử phá |
| D3 | X3: bài đo gọi thẳng `docMucDongDau` — D1 `toThrow`, V1–V5/V7 bắt được; **gỡ luật D1 ⇒ ĐỎ** | thử phá (vòng trước gỡ vẫn 139 xanh) |
| D4 | X3: docstring nói đúng phân vai hai lớp và đúng phạm vi (chỉ trong 9.1) | đọc |
| D5 | X4: bia mộ **không** lọt vế top-10; bia mộ **đã trích** vẫn ở lại | test cả hai chiều; mutant bỏ điều kiện ⇒ ĐỎ |
| D6 | X5: thêm một param `enum` không khai ⇒ **ĐỎ** | thử phá |
| D7 | X6–X10: tên đúng việc, không oracle tự nghiệm, không câu nửa đúng, không prop chết | đọc + test |
| D8 | X11: nợ gom một chỗ, đủ 9 khoản | đọc |
| D9 | Không hồi quy: ≥ 443 Python + ≥ 233 e2e, 0 warning, codegen khớp, lint/build/tsc sạch, SEO ≥ 90; A/B/C vẫn ĐẠT | chạy |
| D10 | `PLAN.md` hash `893b9a2f…`, `CLAUDE.md` hash `f2b4bd73…` **không đổi**; chưa commit; không rác | `git hash-object` |

## 3. Rủi ro
1. **X4 đụng `cau_dang_doc`** — bài đo A5 và B-series dựa vào nó. Đỏ thì dừng và báo.
2. **X1 mở phạm vi bốn cụm cấm ra cả `main`** có thể làm đỏ bài đo khác vì trang có chữ hợp lệ
   chứa cụm đó. Đỏ là **tín hiệu**, đọc kỹ rồi mới quyết, đừng thu hẹp phạm vi lại cho xanh.
3. **Đây là lượt cuối.** Đừng mở mặt trận mới — thấy việc thì ghi nợ.

---

## 4. NỢ CÓ TÊN của Phase 1d — gom một chỗ (X11, sửa ở Y5)

**Mười khoản** của 1d, cộng hai khoản **mang sang từ 1b** ở mục 4b. Mỗi khoản: **nó là gì ·
nằm ở đâu · vì sao hoãn · gỡ khi nào**. Đây là chỗ DUY NHẤT liệt kê đủ; các docstring tại
chỗ vẫn là nơi giữ lý lẽ đầy đủ.

> **Không đánh số** *(Y5, lượt vá 4)*. Bản trước đánh `1…9`, và code đang trỏ tới **một nợ
> khác** bằng đúng cái tên `#1`: `apps/web/app/m/[slugId]/page.tsx` và
> `apps/web/lib/song-song.ts` viết *"nợ có tên #1 (N+1 ngăn kéo)"* — đó là khoản
> `N+1-NGAN-KEO` dưới đây, không phải khoản keyset. Người lần theo dấu từ code đọc trúng
> khoản khác hẳn. Từ nay mỗi khoản có **tên định danh** viết hoa, và code trỏ bằng tên ấy.

**`KEYSET-BIEN-DOI` — keyset của `?sort=nhieu_diem` chạy trên một khoá BIẾN ĐỔI.**
`api/api/feeds.py::_trang_diem` · ghim ở `api/tests/test_keyset_khoa_bien_doi.py`.
`diem_bai_goc` đổi khi có người vote, nên trong một lượt cuộn có mạch **bị sót hẳn** hoặc
**hiện hai lần**. Đã chốt KHÔNG chữa: OFFSET tệ hơn, snapshot là một hệ thống khác. Hai bài
đo ở đó là **tài liệu hoá một đánh đổi**, không phải bảo vệ một tính chất. Gỡ khi nào có yêu
cầu thật về tính ổn định của phân trang — không phải ở Phase 2.

**`DONG-BO-DIEM` — `_dong_bo_diem_bai_goc` tranh chấp khoá, chưa tối ưu.**
`api/core/ghi.py:508` (phần nợ ở `526-552`).
Mỗi lá phiếu vào mốc 1 lấy **khoá độc quyền hàng `Mach`** rồi chạy **5 truy vấn tổng hợp**,
trong đó bốn con số không liên quan gì tới lá phiếu vừa bỏ. Bài càng nóng, hàng đợi càng dài.
Phương án đã viết sẵn: một `UPDATE` đúng một cột dưới khoá hàng `Moc`. Hoãn vì **Phase 2 mới
có endpoint vote để đo** — đổi bây giờ là tối ưu mù.
Gỡ ở **Phase 2**, ngay sau khi endpoint vote chạy và có số đo tranh chấp thật.

**`NAV-GHI-CUNG` — nav `chrome.tsx` ghi cứng hai slug sub, PLAN mục 7 cấm.**
`apps/web/components/chrome.tsx:8-11` (slug ở `32-34`) · giấy miễn trừ song sinh ở
`apps/web/e2e/don-vi/khong-ghi-cung-sub.spec.ts::CHUA_CHUYEN_DUOC`.
Sub thứ ba mở qua admin **vắng mặt trên nav của MỌI trang**, im lặng, 200 ở mọi cửa; đổi slug
thì mục nav trỏ 404. Chưa gỡ được vì component nằm trong layout gốc nên render cả trên
`/luat`, mà `/luat` **phải** là route tĩnh (nó là đường thoát của `error.tsx`). Gỡ ở **Phase
3**, bằng nguồn sub không đi qua request (ISR/cache build-time) hoặc tách nav khỏi layout gốc.

**`INDEX-TOP-KHOANG` — "top theo khoảng" chưa có index phục vụ được cả lọc lẫn sắp.**
`api/core/models/dien_dan.py:197-201` (lý lẽ `EXPLAIN` ở `168-195`).
`?sort=nhieu_diem&khoang=…` luôn phải `Sort` trong bộ nhớ, và vế keyset của trang 2 rơi xuống
`Filter` chứ không thành `Index Cond`. Ba cách chữa (index partial theo từng khoảng · BRIN +
sắp trong bộ nhớ · chấp nhận `Sort`) **cần số đo trên dữ liệu THẬT** — 24 hàng không phân biệt
được chúng. Gỡ khi có dữ liệu đủ lớn để `EXPLAIN ANALYZE` nói được điều gì.

**`BAM-MUC-91` — băm 9.1 là chữ ký của một CON NGƯỜI, không phải một phép chứng minh.**
`apps/web/e2e/don-vi/mau-token.spec.ts` (khai ở khối `BAM_MUC_91`).
Lớp 2 băm nguyên văn PLAN 9.1; nó **không** kiểm được rằng allowlist thật sự suy từ 9.1. Ai
dán băm mới cho hết đỏ là qua cửa. Hàng rào chỉ ép người ta **cố ý**, không ép **đúng** — và
đó là trần của cách tiếp cận này, không phải một lỗi cài đặt. Không có kế hoạch gỡ.

**`LOP-1-MU` — lớp 1 của hàng rào 9.1 MÙ với D2 và D3.**
`apps/web/e2e/don-vi/mau-token.spec.ts` — nói ra ở khối "PHÂN VAI THẬT", đo ở
`test("X3 — chỗ lớp 1 MÙ: D2 và D3 chỉ có lớp băm bắt được")`.
`docMucDongDau` không nhận ra "một câu mới sau `**Danh sách này là NGUỒN…`" (D2) và "một gạch
đầu dòng mới ngay dưới" (D3) là mục đóng dấu thứ 8. Hai cửa ấy hôm nay **chỉ có lớp băm
chặn** ⇒ **gỡ `BAM_MUC_91` là mở lại đúng hai cửa này**. Hoãn có chủ đích: đây là lượt vá thứ
ba của cùng một hàng rào, và hướng "đọc hiểu văn xuôi tiếng Việt bằng regex" không có đáy.
**Gỡ khi nào: không có kế hoạch gỡ** *(Y5)* — hai cửa ấy cố ý sống nhờ lớp băm, và điều
kiện để gỡ nợ này là một cách kiểm khác hẳn (ví dụ 9.1 chuyển sang một định dạng máy đọc
được), không phải một lượt siết regex nữa.

**`WILSON-DOCSTRING` — docstring `wilson_lower_bound` khẳng định một đẳng thức chỉ đúng
trên giấy.**
`api/core/xep_hang.py:27-30` nói `up = 0, down = n` → `0.0` với **mọi** `n`. Cài đặt float thì
không: `wilson(0, 5) = 2.09e-17`, `wilson(0, 20) = 6.41e-18`, và dấu **lật** ở `down ≳ 1000`
(`wilson(0, 1000) = −1.08e-19`). Đã vá ở **tầng sắp xếp** (`_cat_du_so` + khoá `score`, nay có
phép quét thật ở `tests/test_pha_hoa_wilson.py` — X2), và cố ý **không đụng vào hàm**: nó là
công thức của PLAN 5.3. Còn nợ đúng một việc: **câu trong `xep_hang.py` chưa nói ra sự suy
biến đó**, nên người đọc hàm vẫn tin cái đẳng thức. Gỡ bằng một dòng docstring, bất cứ lúc nào.

**`DANG-DOC-ROUND-TRIP` — `?dang_doc=1` tốn thêm một round-trip mỗi lần bung khán đài.**
`apps/web/lib/api.ts::docCauDangDoc` · gọi ở `apps/web/app/m/[slugId]/page.tsx`.
Lời gọi RIÊNG, không tái dụng trang khán đài đang hiện, vì hai thứ có **thứ tự khác nhau**
(wilson thuần vs. sort người dùng chọn) và tập này không phân trang. Gộp lại sẽ âm thầm biến
"câu đáng đọc" thành "10 dòng đầu của trang đang xem" — hỏng im lặng, nên chấp nhận trả giá
một lượt gọi.
Ghi chú tại chỗ **đã có** ở cả hai đầu (docstring `docCauDangDoc` và comment ngay trên lời
gọi) và cả hai nêu đúng lý lẽ trên; cái chúng chưa nói là **cái giá** — một round-trip mỗi
lượt bung. *(Câu cũ ở đây viết "nợ này chưa có ghi chú tại chỗ trong code" — sai, sửa ở
Y5.)*
**Gỡ khi nào: không có kế hoạch gỡ** ở dạng "gộp hai lời gọi". Cái giá này biến mất theo
một đường khác — ISR/cache ở Phase 3 làm cả trang mạch không còn gọi Django mỗi lượt tải.

**`MAT-DO-DONG` — "nén mật độ dòng theo 9.1" không có tiêu chí đo được.**
Hạng mục 8 của `plans/2026-08-22-phase-1d-vo-reddit.md:64` · cài đặt ở
`apps/web/components/the-mach.module.css:3-6`, chỉ có văn xuôi.
Bảng nghiệm thu A1–A16 của plan 1d **không có tiêu chí nào** cho hạng mục này, và không bài đo
nào chạm tới `line-height` hay số thẻ lọt trong một viewport. Tức "nén mật độ" là ý kiến chủ
quan: không chứng minh được đạt, và một lần regress padding sẽ **không làm đỏ gì**. Gỡ bằng
cách chọn một con số đo được (ví dụ: số thẻ hiện đủ trong viewport 375×812) và ghim nó — việc
của lượt sau, vì chốt một con số như thế là một **quyết định thiết kế**, không phải một bản vá.

**`N+1-NGAN-KEO` — trang mạch gọi Django một lần cho MỖI mốc** *(bổ sung ở Y5; đây là khoản
mà hai chỗ trong code vẫn gọi bằng cái tên cũ `#1`)*.
`apps/web/app/m/[slugId]/page.tsx` (vòng `chayCoTran(mach.mocs, …)`) · trần đồng thời ở
`apps/web/lib/song-song.ts::TRAN_NGAN_KEO` · nêu lần đầu ở `plans/2026-08-22-phase-1c-va.md` §5
(khoản 1), nhắc lại ở `plans/2026-08-22-phase-1c-va2.md` §5. *(Sửa 2026-08-22 ở chặng 5: bản trước trỏ
`phase-1d-va.md` §5 — file đó chỉ có §0…§3, không có §5.)*
Ngăn kéo nạp sẵn cho **mọi** mốc, kể cả mốc `so_binh_luan === 0` (vá B1) và mốc nằm trong
dải gập, nên mạch 40 mốc là 40 lời gọi cho một lượt tải một URL công khai. Vá E4 chỉ hạ
**độ đồng thời** xuống 6 — số lời gọi không đổi, và đó là chỗ nợ còn nguyên.
Hoãn vì phương án thật là một endpoint trả cả rừng lát cắt (hoặc ISR để cả trang không đi
xuống Django mỗi lượt), tức một thay đổi hợp đồng API chứ không phải một bản vá render.
Gỡ ở **Phase 3, cùng lúc bật ISR** — đúng câu đang viết ở cả hai chỗ trong code.

**`DAU-CHU-NGUOI-DUNG-MOT-CHIEU` — hàng rào Y3 chỉ có răng một chiều** *(nghiệm thu vòng
5 đo được)*. `apps/web/e2e/vo-reddit.spec.ts::chuCuaUngDung` trừ mọi nút mang
`data-chu-nguoi-dung` ra khỏi phép quét. **Gỡ** dấu khỏi một nút chữ người dùng ⇒ ĐỎ đúng
(đã thử phá). **Thêm** dấu nhầm lên một nút chữ ỨNG DỤNG ⇒ `5 passed`, im lặng — nó lặng lẽ
đục một điểm mù vào hàng rào và không gì kêu, vì trên trang thật `chuCuaUngDung` chỉ bị soi
bằng các khẳng định PHỦ ĐỊNH (trừ mất chữ thì vẫn xanh).
Hoãn vì bịt nó cần một bài đo dạng "tập nút mang dấu phải BẰNG ĐÚNG danh sách khai", tức
một hàng rào cho hàng rào — đáng làm, nhưng không đáng giữ phase lại thêm một vòng.
Gỡ khi Y3 được mở phạm vi sang trang mạch (lúc đó số nút mang dấu tăng mạnh).

**`KHOI-DANG-DOC-GOC-AN` — `so_ung_vien_bo_lai = 0` không có nghĩa "tập chứa trọn phần
đọc được của cây"** *(phản biện vòng 5)*. Nó chỉ nói về **thread GỐC** đọc được. Hình dạng
cực đoan: 3 gốc đọc được + 40 gốc bị mod ẩn mà mỗi gốc còn nhánh con đọc được ⇒ cả 40 thread
nằm ngoài tập nhưng `so_ung_vien_bo_lai = 0` ⇒ **ẩn khối**, trong khi khối ấy đang lọc thật.
Mất một lối tắt, KHÔNG mất nội dung (cây đầy đủ nằm ngay dưới) — nên là nợ, không phải lỗi.
Bốn câu docstring nói quá đã sửa ở chặng 5. Gỡ khi có ca thật: điều kiện đúng là "mọi thread
gốc của cây đều có mặt trong tập", đắt hơn một con số nguyên và chưa đáng ở 1d.

### 4b. Hai khoản MANG SANG từ Phase 1b — cố ý không nằm trong danh sách trên

> **`1b #N` = `plans/2026-08-22-phase-1b-va2.md` §3.** Ghi ra vì token đó từng trỏ vào
> **hai** hệ đánh số: `plans/2026-08-22-phase-1c-frontend-can.md` §5 đánh lại từ 1, nên
> cùng chuỗi `1b #4` đang mô tả hai khoản khác nhau ở `khan-dai.ts` và `moi-bung.ts`.
> Đã sửa `khan-dai.ts` thành `1b #3` theo hệ chuẩn *(chặng 5, 2026-08-22)*. Đây đúng là
> loài lỗi mà Y5 dựng ra để diệt — Y5 chỉ chữa `#1` và bỏ sót `#4`.

Chúng là nợ **của 1b**, không phải của 1d, nên §4 không đếm chúng vào — nhưng code 1d vẫn
trỏ tới bằng tên `1b #6` / `1b #8`, và một danh sách tự nhận là "chỗ DUY NHẤT" mà bỏ trắng
hai cái tên ấy thì người lần theo dấu lại đi vào ngõ cụt. Trả lời câu hỏi của Y5: **cố ý bỏ,
không phải bỏ sót** — ghi ở đây để hai cái tên có chỗ đáp.

- **`1b #6` — hồ sơ CẮT ở `limit`, không có cursor.** `api/api/users.py::SO_MACH_TREN_HO_SO`
  (20) · UI nói ra ở `apps/web/app/u/[username]/page.tsx` (`ho-so-bi-cat`). Phần dôi ra
  không có đường lấy tiếp; 1d chỉ làm được vế "nói ra bằng chữ thay vì cắt âm thầm".
  Gỡ khi có người dùng thật vượt 20 mạch, bằng đúng cơ chế keyset của feed.
- **`1b #8` — deep-link từ khối trích có thể trỏ vào trang sau của khán đài.**
  `apps/web/lib/khan-dai.ts::trangThaiDeepLink` · dùng ở `apps/web/components/khoi-trich.tsx`.
  `hay_nhat` chỉ trả 50 thread gốc đầu, nên bình luận được trích ở mạch lớn có thể không
  nằm trong trang đang tải; 1d chỉ làm vế "nói ra" (`"trang_sau"`), không làm vế "nhảy tới
  đúng trang chứa nó". Gỡ khi khán đài có đường mở thẳng tới một `comment_id` (Phase 3,
  cùng lúc với `/machs/{id}/me`).
