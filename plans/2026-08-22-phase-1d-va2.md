# Plan con — Phase 1d, LƯỢT VÁ 2

> Nguồn: phản biện vòng 2 (1 NẶNG + 4 VỪA + 9 NHỎ) + 1 việc nghiệm thu vòng 2 để lại.
> Ngày 2026-08-22. Bản 1d + vá 1 **chưa commit** — mọi thứ dưới đây vá trên cây đang có.

## 0. Chốt của phiên chính về phát hiện NẶNG (N1)

**Cursor keyset của `?sort=nhieu_diem` neo vào `Mach.diem_bai_goc` — một khoá BIẾN ĐỔI.**
Ai đó vote giữa lúc người khác lật trang thì có mạch **bị sót hẳn**, hoặc **hiện hai lần**.

**Quyết định: GIỮ keyset, KHÔNG đổi cơ chế.** Ba lý do:
1. Mọi bảng xếp hạng theo điểm sống đều có tính chất này — Reddit cũng vậy. Offset **không** chữa
   được (nó còn thêm bệnh trượt hàng của riêng nó), và snapshot là một hệ thống khác hẳn.
2. Hậu quả là một mạch lệch chỗ trong một lần cuộn, không mất dữ liệu, không sai vĩnh viễn.
3. Cái giá của "chữa" nó lớn hơn nhiều lần thứ nó chữa.

**Nhưng chỗ SAI THẬT thì phải vá**: `api/api/phan_trang.py:3-11` đang khẳng định
> *"Keyset neo vào giá trị thật của hàng cuối cùng đã trả, nên hai ca đó không xảy ra."*

Câu ấy đúng khi mọi khoá còn bất biến, và **1d đã làm nó sai** mà không ai sửa. Đây là loài lỗi
số 1 của repo, nằm ngay trong file dựng ra để chống đúng bệnh đó.

## 1. Mười ba hạng mục

### W1 — N1: sửa lời khẳng định, ghim hành vi thật *(NẶNG)*
- `api/api/phan_trang.py` docstring module: nói rõ bảo đảm "không trùng, không sót" **chỉ đúng
  với khoá BẤT BIẾN** (`created_at`, `last_entry_at`), và **không** đúng với `diem_bai_goc`.
- `ma_hoa_cursor_so` + `_trang_diem` (`api/api/feeds.py:91-116`): nợ có tên tại chỗ, kèm **cả hai
  ca** (sót khi điểm tăng vượt mốc; trùng khi điểm tụt xuống dưới mốc).
- **Ghim bằng test**: dựng đúng hai ca, khẳng định hành vi hiện tại. Đây là test *tài liệu hoá* —
  ghi rõ trong docstring test rằng nó ghim một **đánh đổi đã chốt**, không phải một tính chất mong
  muốn, để ngày ai đó đổi cơ chế thì nó đỏ và người ta đọc được vì sao.

### W2 — hàng rào `type-frontend.spec.ts` bị nới thành mù *(VỪA, đã chứng minh bằng chạy thật)*
`TU_TRU_THEO_FILE` lọc bỏ **cả file** khỏi **cả 6 luật**, trong đó có *"không import `client`
singleton"* và *"mọi lời gọi API phải có `baseUrl`"* — đúng đường rò session user A sang user B mà
`gikky-net/CLAUDE.md` cấm. Phản biện thêm 2 lời gọi thiếu `baseUrl` vào file được miễn trừ ⇒
**120 passed, 0 failed**. Nghiệm thu đo độc lập, cùng kết quả.
Miễn trừ chỉ **cần** cho đúng một luật (`lietKeSub` xuất hiện trong regex literal).
⇒ **Miễn trừ theo LUẬT, không theo FILE.** Hoặc bỏ hẳn nhu cầu miễn trừ (ghép chuỗi trong spec).

### W3 — hàng rào đọc `PLAN.md` VẪN fail-OPEN, ba cách *(VỪA)*
Phản biện chạy 11 biến thể trên **bản chép**. 8 fail-closed đúng. Ba cái **XANH**:
- **D1** — viết mục mới **trong ngoặc chú thích**: `*(ba cái sau chốt 2026-08-22; cộng **X** từ
  2026-09-01)*`. **Cửa này do chính V4 mở** (`.replaceAll(/\*\([^)]*\)\*/g, "")`), và nó là cách
  viết tự nhiên nhất vì ba mục hiện tại đều được thêm kèm đúng một cái ngoặc như vậy.
- **D2** — câu mới **sau** `**Danh sách này là NGUỒN`, cùng gạch đầu dòng.
- **D3** — gạch đầu dòng mới ngay dưới.

**Tiêu chí là ma trận, không phải một ca:** cả 11 biến thể phải **ĐỎ** (đỏ đúng hoặc đỏ ồn ào đều
được — không được XANH). Gợi ý hướng: cắt tới hết **gạch đầu dòng** (`\n- `) thay vì tới một chuỗi
giữa đoạn; **đừng xoá ngoặc** — thay bằng "phần bị bỏ đi phải được ĐẾM và khẳng định không chứa
cụm bold nào chưa khai".
⚠ Đây là lượt thứ **hai** vá cùng một hàng rào. Nếu bạn thấy cách tiếp cận "phân tích văn xuôi
tiếng Việt bằng regex" là sai từ gốc thì **nói ra** kèm đề xuất — đừng vá lần thứ ba cùng kiểu.

### W4 — hai chỗ thay số `0` trung thực bằng một CÂU SAI *(VỪA)*
V6 và A12 cùng dính: `so_mach` / `so_binh_luan` đếm nội dung **hiện được**, nên
- sub từng có bài rồi **bị mod ẩn hết** ⇒ in *"Chuyên mục mới · lập 04/03/2024"* — hai vế tự mâu
  thuẫn trên một dòng;
- user bị ẩn sạch bài ⇒ *"Tài khoản này chưa đăng mạch hay bình luận nào."*

`×0` cũ ít nhất không nói gì thêm; câu mới là **khẳng định sai về dữ liệu**.
⇒ Đổi sang câu **đúng trong cả hai ca** (không quả quyết "mới"/"chưa từng"), và **không** rò ra
việc có nội dung bị ẩn. Không cần đổi API.
Kèm: `vo-reddit.spec.ts:316-330` đo B8 **bằng phủ định** (`not.toContainText("0 mạch")`) — mutant
xoá hẳn chữ "Chuyên mục mới" vẫn xanh. Thêm khẳng định dương.

### W5 — tranh chấp khoá của `_dong_bo_diem_bai_goc` *(VỪA — chỉ ghi nợ, KHÔNG đổi hành vi)*
Mỗi phiếu vào mốc 1 khoá độc quyền hàng `Mach` + 5 truy vấn tổng hợp, và **nối tiếp với**
`tao_binh_luan`/`them_moc` trên cùng hàng đó — bài càng nóng càng đau.
**Không tối ưu bây giờ** (Phase 2 mới có endpoint vote, tối ưu mù thì không đo được). Nhưng
docstring hiện nói nhiều về thứ tự khoá mà **không một chữ** về tranh chấp ⇒ thêm nợ có tên, kèm
đúng phương án rẻ hơn để Phase 2 cân: `Mach.objects.filter(pk=…).update(diem_bai_goc=…)` trong
cùng transaction (hàng `Moc` đã khoá nên vẫn tuần tự hoá được), thay vì đếm lại 4 cột không liên quan.

### W6 — `_rank_goc` còn nguyên sự đảo wilson *(cửa anh em của V9)*
`api/core/doc_noi_dung.py:273-281` vẫn so wilson thô ⇒ ở sort **mặc định** của khán đài, câu bị
dìm vẫn xếp trên câu chưa ai vote. Áp `_khoa_wilson` ở đây nữa.
⚠ Đây là **đổi thứ tự khán đài mặc định** — bài đo 1b/1c có thể đỏ. **Đỏ thì dừng và báo**, đừng
sửa số cho khớp.
Kèm: docstring `_rank_goc` nói *"bình luận đọc được bị dìm nặng vẫn xếp trên [bia mộ]"* như một
**lựa chọn**; thực tế đó là dư số, và với `down ≳ 1000` dấu dư số **lật** (`wilson(0,1000)` âm) nên
câu đó còn sai luôn. Nếu "đọc được xếp trên bia mộ" là ý muốn thật thì hãy làm nó bằng một khoá
tường minh, đừng để nó dựa vào nhiễu dấu phẩy động.

### W7 — `CHU_SO_SO_WILSON`: kết luận đúng, con số biện minh sai
Docstring nói khoảng cách thật *"nhỏ nhất cũng cỡ `1e-3`"* và *"chín bậc mười trống"*. Phản biện
quét `up,down ∈ [0,200]`: sàn thật **3.94e-10** (`(134,15)` vs `(178,21)`); ở `n = 10⁹` là
**5.0e-10**. Biên an toàn là **~2,4 bậc**, không phải 9. (Phép cắt vẫn an toàn: 0 cặp `up>0` bị
gộp nhầm trong 40 401 giá trị.) Sửa con số, giữ kết luận.

### W8 — B11 "hay_nhat không hồi quy" là ĐẠT nhưng RỖNG
Cả 14 bình luận gốc của seed đều `up > 0` và 14 giá trị wilson phân biệt ⇒ **không có cú hoà nào**
⇒ khoá `-n.diem` không bao giờ được hỏi tới trên seed. Thêm bài đo có hoà thật.
Kèm: `test_api_cau_dang_doc.py:49,66` và `test_seed_dev.py:184,214,259` dùng `wilson_lower_bound`
**thô** làm oracle — một bộ so sánh **khác** với code. Ngày một bình luận gốc về `up = 0` thì test
đỏ vì "code đúng, oracle cũ". Cho oracle dùng đúng khoá của code, hoặc ghi rõ nó đo cái gì.

### W9 — `KhoangThoiGian` là hằng CHẾT, và frontend đang KHAI LẠI hợp đồng API
`api/core/thoi_gian.py:29` khai `Literal[...]` nhưng **không ai import**. Endpoint nhận
`khoang: str` / `sort: str` ⇒ `openapi.json` ra `string` ⇒ `apps/web/lib/api.ts:296-305` phải
**tự khai lại** `KHOANG_FEED` và `SORT_API`.
Đó là **bản sao thứ hai của hợp đồng API** — đúng thứ `PLAN.md` 8.3 và `gikky-net/CLAUDE.md` cấm
("frontend cấm tự khai interface trùng với API"), và `type-frontend.spec.ts` không bắt được vì nó
chỉ soi **tên schema**.
⇒ Dùng `Literal[...]` ở chữ ký endpoint để OpenAPI ra enum, `pnpm codegen`, **xoá** hai bản khai
tay ở frontend. Cân nhắc thêm một luật vào `type-frontend.spec.ts` bắt loài này.

### W10 — `mach_created_diem`: cột thứ hai không làm gì, tên index nói ngược
`EXPLAIN` (cả thợ lẫn phản biện lẫn nghiệm thu đều đo): index `(-created_at)` một cột cho **y hệt**
kế hoạch — `diem_bai_goc` chỉ rơi vào `Filter`, và index **không có cột `id`** nên không bao giờ
phục vụ được `ORDER BY diem DESC, id DESC` kể cả khi vị từ là `=`.
**Migration `0005` CHƯA COMMIT** ⇒ sửa thẳng nó được, không cần `0006`. Chọn một:
(a) bỏ cột thứ hai + đổi tên index cho đúng; (b) giữ, nhưng chú thích phải nói rõ nó **không** đóng
góp gì vào kế hoạch đã đo. Kèm: mọi số `EXPLAIN` dán vào chú thích phải ghi **đo trên 24 hàng**.

### W11 — `data-binh-luan-id` trùng trong DOM
`CauDangDoc` render lại tới 10 thread **ở trên** cây đầy đủ; `binh-luan.tsx:61-66` gắn
`data-testid="binh-luan"` + `data-binh-luan-id` **vô điều kiện** ⇒ mỗi bình luận "đáng đọc" có hai
nút DOM cùng thuộc tính. E2E hiện sống sót vì mọi locator đều scope — nhưng bất biến "một nút /
một comment id" đã mất, và cái bẫy này chỉ nổ ở bài đo viết sau.

### W12 — docstring `SubChiTietOut` sai
`api/api/schemas.py:100-106` nói `so_mach` là *"đúng bộ lọc mà hai feed dùng"*;
`/feeds/dang-dien-ra` lọc thêm `status=MO`.

### W13 — `chrome.tsx` là bản sao thứ ba, và giấy miễn trừ nằm ở file khác
`apps/web/components/chrome.tsx:15-16` còn ghi cứng `/s/chung-khoan` + `/s/crypto` ⇒ **sub thứ ba
mở ra vẫn vắng mặt trên thanh nav của mọi trang**. Lý lẽ hoãn **đúng và đã kiểm** (`Chrome` ở layout
gốc; `/luat` phải TĨNH vì nó là đường thoát của `error.tsx`; `pnpm build` xác nhận `○ /luat`), nên
**không chuyển sang `GET /subs` ở lượt này**. Nhưng docstring của **chính file đó** không nói gì —
nợ phải nằm ở nơi người sửa file sẽ đọc, không phải ở một spec khác. Ghi nợ tại chỗ, trỏ Phase 3
(ISR, PLAN 8.4).

## 2. Tiêu chí nghiệm thu

| # | Tiêu chí | Cách đo |
|---|---|---|
| C1 | W1: câu tuyệt đối ở `phan_trang.py` đã sửa; nợ có tên ở cả 2 chỗ; **2 test ghim cả ca sót lẫn ca trùng** | đọc + test |
| C2 | W2: miễn trừ theo LUẬT. **Thêm `lietKeSub({})` và `xemMach({path:{mach_id:1}})` (không `baseUrl`) vào file được miễn trừ ⇒ ĐỎ** | thử phá — đây là bài đo quyết định |
| C3 | W3: **cả 11 biến thể ĐỎ**, gồm D1/D2/D3 đang xanh | thử phá trên **bản chép**, `PLAN.md` thật không đụng |
| C4 | W4: sub bị ẩn hết bài **không** bị gọi là "mới"; user bị ẩn hết bài **không** bị nói là chưa từng đăng; không rò việc có nội dung bị ẩn | test dựng cả 2 ca |
| C5 | W4: B8 có khẳng định **dương**; mutant xoá chữ thay thế ⇒ ĐỎ | thử phá |
| C6 | W5: nợ có tên + phương án Phase 2; **hành vi KHÔNG đổi** | đọc + test cũ vẫn xanh |
| C7 | W6: câu bị dìm không chen trên câu chưa ai vote **ở sort mặc định**; mutant bỏ ⇒ ĐỎ | test |
| C8 | W6: nếu "đọc được trên bia mộ" là ý muốn thì có khoá **tường minh**, không dựa dư số | đọc + test |
| C9 | W7: con số trong docstring khớp phép đo | đọc + script |
| C10 | W8: có bài đo **hoà thật**; mutant bỏ khoá `-n.diem` ⇒ ĐỎ; oracle không còn lệch với code | test |
| C11 | W9: `openapi.json` ra **enum** cho `khoang`/`sort`; `KHOANG_FEED`/`SORT_API` khai tay **biến mất**; `codegen:check` khớp | `grep` + codegen |
| C12 | W10: chú thích khớp `EXPLAIN` và ghi rõ đo trên 24 hàng; nếu bỏ cột thì `0005` sửa tại chỗ, **không đẻ `0006`** | đọc + `EXPLAIN` |
| C13 | W11: một comment id ⇒ **một** nút mang `data-binh-luan-id` | Playwright đếm |
| C14 | W12 + W13: docstring đúng; nợ nav nằm **trong** `chrome.tsx` | đọc |
| C15 | Không hồi quy: ≥ 434 Python + ≥ 212 e2e, 0 warning, codegen khớp, lint/build/tsc sạch, SEO ≥ 90; **A1–A16 và B1–B16 vẫn ĐẠT** | chạy |
| C16 | Chưa commit; không rác; `PLAN.md`/`CLAUDE.md` **không bị sửa**; `seed_dev --reset` chạy được | `git hash-object` + chạy |

## 3. Rủi ro
1. **W6 đổi thứ tự khán đài mặc định.** Đỏ thì dừng và báo.
2. **W9 đụng OpenAPI ⇒ TS client sinh lại.** Chữ ký đổi có thể làm đỏ bài đo hợp đồng — đó là
   **đúng**, sửa cho khớp enum mới, nhưng đừng nới hàng rào để đi qua.
3. **W3 là lượt vá thứ HAI của cùng một hàng rào.** Nếu cách tiếp cận sai từ gốc thì nói ra.
4. **W2 và W3 đều là "sửa bài đo".** Bài đo sửa xong phải **đỏ được** — chứng minh bằng thử phá,
   đừng chỉ chạy cho nó xanh.
