# `eslint-config-next` nạp plugin bằng require() từ thư mục app. Với node_modules
# cô lập của pnpm, các plugin đó nằm trong .pnpm và app không thấy -> eslint chết
# ("Cannot find module 'eslint-plugin-react-hooks'"). Hoist công khai nhóm eslint.
public-hoist-pattern[]=*eslint*
