# ============================================================
# Gikky-net Docker environment — copy to .env before deploying
# cp .env.docker .env
# ============================================================

# --- PostgreSQL connection ---
# Option A: Postgres is in another Docker container
#   DATABASE_HOST=<postgres-container-name>
#   (and add that container's network to gikky-api in docker-compose.yml)
# Option B: Postgres is on the VPS host (outside Docker)
#   DATABASE_HOST=host.docker.internal   ← works on Linux with extra_hosts above
DATABASE_HOST=host.docker.internal
DATABASE_PORT=5432
DATABASE_NAME=gikky_strapi
DATABASE_USERNAME=your_db_user
DATABASE_PASSWORD=your_db_password

# --- Strapi secrets (generate random strings) ---
# Use: openssl rand -base64 32
APP_KEYS=key1_change_me,key2_change_me
API_TOKEN_SALT=change_me
ADMIN_JWT_SECRET=change_me
TRANSFER_TOKEN_SALT=change_me
JWT_SECRET=change_me
ENCRYPTION_KEY=change_me

# --- Frontend API URL (how web/refne reach Strapi) ---
# Inside Docker network, use container name
NEXT_PUBLIC_API_URL=http://gikky-api:1337
